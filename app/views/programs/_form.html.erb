<%= form_with(model: program, local: true) do |form| %>
  <% if program.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(program.errors.count, "error") %> prohibited this program from being saved:</h2>

      <ul>
      <% program.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :program_name %>
  </div>

  <div class="field">
    <%= form.label :faculty_id %>
    <%= form.number_field :faculty_id, id: :program_faculty_id %>
  </div>

  <div class="field">
    <%= form.label :open_space %>
    <%= form.number_field :open_space, id: :program_open_space %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
